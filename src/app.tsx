/**
 *
 * 覆盖文件
 */

const configureParams = () => ({
  // hzer-ui 弹窗移动全局配置属性
  modalMovable: true,
  // hzero-ui 弹窗居中全局配置数据
  modalAutoCenter: true,
  // notification 关闭延时时间
  // notificationDuration: 6,
});

export { configureParams };
